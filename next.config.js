/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['your-image-host.com'], // Replace with the actual domain where your logo is hosted
  },
}

module.exports = nextConfig

